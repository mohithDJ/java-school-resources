import java.util.*;
public class Exchange
{
    String sent,w;
    int len;
    Scanner in = new Scanner (System.in);
    void input()
    {
        System.out.println("Enter the Sentence.");
        sent=in.next();
        len=sent.length();
        for(int i=0;i<len;i++)
        {
            
        }
    }
}